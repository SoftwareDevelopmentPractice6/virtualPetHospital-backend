server:
  port: 8777
spring:
  application:
    name: medicalRecordManagement

eureka:
  instance:
    lease-renewal-interval-in-seconds: 30      # ??????????????????30s?
    lease-expiration-duration-in-seconds: 90  # ??????????????????90s?
  client:
    registry-fetch-interval-seconds: 30 # ??????????????30s?
    service-url:
      defaultZone: http://localhost:5272/eureka/
    healthcheck:
      enabled: true # ?????????spring-boot-starter-actuator?

